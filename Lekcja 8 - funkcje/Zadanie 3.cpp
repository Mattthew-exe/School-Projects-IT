#include <iostream>
#include <cstring>
using namespace std;
		string GetTextFromUser(){
			string text;
			string reversed;
			getline(cin, text);
			return text;
		}
			string ReverseString(char text[]){
					string reversed;
					strlen(text);
				}
int main(){
	string text;
	cout << "podaj ciag znakow: ";
	text = GetTextFromUser();
}